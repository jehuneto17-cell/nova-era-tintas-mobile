/** Chave de sessionStorage usada para passar a cor escolhida entre a página do produto e a tela de seleção de cor. */
export const corEscolhidaKey = (produtoId: string) => `ne-cor-escolhida:${produtoId}`;
